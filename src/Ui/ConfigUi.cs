namespace Morpheus.Ui;

// Stub — next pass: ImGui.NET overlay.
// Panels: avatar picker (dropdown from avatars/), template picker (dropdown from templates/),
// ElevenLabs key + voice, bind-session button with hook installer, personality install w/ confirm.
public sealed class ConfigUi
{
    public bool Visible { get; set; } = true;
    public void Draw() { }
}
